<h1 class="text-center mb-3">{{title}}</h1>
<h2 class="text-center">Write a subject and I'll give you the latest post about it</h2>
<form action="/api/searches/" method="GET" class="mb4">
    <div class="form-group">
        <label for="keyword" class="collectes-ville text-center"> Write your tag: </label>
        <input type="text" name="keyword" class="form-control">
    </div>
    <input type="submit" value="Find it!" class="btn btn-primary btn-block">
</form>
<h4>last searches:</h4>
<ul class="list-group">
    {{#each searches}}
        <li class="list-group-item">{{this.keyword}}:  <a href="{{this.url}}">{{this.url}}</a></li>
    {{/each}}
</ul>


<a href="https://www.laylita.com/recipes/wp-content/uploads/2013/07/Choripan-recipe.jpg" class="btn btn-light mt-4"> Visit a Great place</a>